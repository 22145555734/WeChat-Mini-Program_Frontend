<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yinnnh.mapper.BookMapper">
    <!-- 按分类查询图书列表 -->
    <select id="selectBooksByCategory" resultType="com.yinnnh.pojo.entity.Book">
        SELECT id, title, author, cover, price, sales, sort, category_id
        FROM book
        WHERE deleted = 0
        AND category_id = #{dto.categoryId}
        <!-- 动态排序：默认按 sort 正序，传 sort=sales 按销量倒序 -->
        <choose>
            <when test="dto.sort != null and dto.sort == 'sales'">
                ORDER BY sales DESC
            </when>
            <otherwise>
                ORDER BY sort ASC
            </otherwise>
        </choose>
    </select>


    <!-- 关键词搜索图书 -->
    <select id="selectSearchBooks" resultType="com.yinnnh.pojo.entity.Book">
        SELECT id, title, author, cover, price, sales, sort, category_id
        FROM book
        WHERE deleted = 0
        <!-- 关键词不为空时，模糊匹配书名/作者 -->
        <if test="dto.keyword != null and dto.keyword.trim() != ''">
            AND (title LIKE CONCAT('%', #{dto.keyword}, '%')
            OR author LIKE CONCAT('%', #{dto.keyword}, '%'))
        </if>
        <!-- 搜索结果默认按销量倒序 -->
        ORDER BY sales DESC
    </select>
</mapper>